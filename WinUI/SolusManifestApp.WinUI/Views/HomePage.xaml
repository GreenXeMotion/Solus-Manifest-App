<Page
    x:Class="SolusManifestApp.WinUI.Views.HomePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <ScrollViewer>
        <StackPanel Padding="40" Spacing="24" MaxWidth="1200">
            <!-- Welcome Header -->
            <StackPanel Spacing="8">
                <TextBlock
                    Text="Welcome to Solus Manifest App"
                    Style="{StaticResource TitleTextBlockStyle}"/>
                <TextBlock
                    Text="Manage Steam depots, manifests, and GreenLuma 2024 integration"
                    Style="{StaticResource BodyTextBlockStyle}"
                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
            </StackPanel>

            <!-- Quick Actions -->
            <StackPanel Spacing="16">
                <TextBlock
                    Text="Quick Actions"
                    Style="{StaticResource SubtitleTextBlockStyle}"/>

                <Grid ColumnSpacing="16" RowSpacing="16">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Launch Steam Card -->
                    <Border
                        Grid.Column="0"
                        Background="{ThemeResource CardBackgroundAcrylicBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="20">
                        <Border.Shadow>
                            <ThemeShadow />
                        </Border.Shadow>
                        <Border.Translation>0,0,8</Border.Translation>
                        <StackPanel Spacing="12">
                            <FontIcon
                                Glyph="&#xE8FC;"
                                FontSize="32"
                                Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                            <TextBlock
                                Text="Launch Steam"
                                Style="{StaticResource SubtitleTextBlockStyle}"/>
                            <TextBlock
                                Text="Start Steam client"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <Button
                                Content="Launch"
                                Command="{Binding LaunchSteamCommand}"
                                HorizontalAlignment="Stretch"/>
                        </StackPanel>
                    </Border>

                    <!-- Refresh Library Card -->
                    <Border
                        Grid.Column="1"
                        Background="{ThemeResource CardBackgroundAcrylicBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="20">
                        <Border.Shadow>
                            <ThemeShadow />
                        </Border.Shadow>
                        <Border.Translation>0,0,8</Border.Translation>
                        <StackPanel Spacing="12">
                            <FontIcon
                                Glyph="&#xE72C;"
                                FontSize="32"
                                Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                            <TextBlock
                                Text="Refresh Library"
                                Style="{StaticResource SubtitleTextBlockStyle}"/>
                            <TextBlock
                                Text="Update game library"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <Button
                                Content="Refresh"
                                Command="{Binding RefreshLibraryCommand}"
                                HorizontalAlignment="Stretch"/>
                        </StackPanel>
                    </Border>

                    <!-- Settings Card -->
                    <Border
                        Grid.Column="2"
                        Background="{ThemeResource CardBackgroundAcrylicBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="20">
                        <Border.Shadow>
                            <ThemeShadow />
                        </Border.Shadow>
                        <Border.Translation>0,0,8</Border.Translation>
                        <StackPanel Spacing="12">
                            <FontIcon
                                Glyph="&#xE713;"
                                FontSize="32"
                                Foreground="{ThemeResource AccentFillColorDefaultBrush}"/>
                            <TextBlock
                                Text="Settings"
                                Style="{StaticResource SubtitleTextBlockStyle}"/>
                            <TextBlock
                                Text="Configure app settings"
                                Style="{StaticResource CaptionTextBlockStyle}"
                                Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <Button
                                Content="Configure"
                                HorizontalAlignment="Stretch"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </StackPanel>

            <!-- System Info -->
            <StackPanel Spacing="12">
                <TextBlock
                    Text="System Information"
                    Style="{StaticResource SubtitleTextBlockStyle}"/>

                <Border
                    Background="{ThemeResource CardBackgroundAcrylicBrush}"
                    BorderBrush="{ThemeResource CardStrokeColorBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="20">
                    <Border.Shadow>
                        <ThemeShadow />
                    </Border.Shadow>
                    <Border.Translation>0,0,8</Border.Translation>
                    <StackPanel Spacing="8">
                        <TextBlock>
                            <Run Text="Version:" FontWeight="SemiBold"/>
                            <Run Text=" 3.0.0"/>
                        </TextBlock>
                        <TextBlock>
                            <Run Text="Framework:" FontWeight="SemiBold"/>
                            <Run Text=" WinUI 3 (.NET 8)"/>
                        </TextBlock>
                        <TextBlock>
                            <Run Text="Status:" FontWeight="SemiBold"/>
                            <Run Text=" Migration in progress"/>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</Page>
